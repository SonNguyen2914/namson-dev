Drop official league marks here to replace the built-in recreations:
  wc26.svg  mls.svg  epl.svg  laliga.svg
SVG preferred (PNG works if named .svg via rename or add a source switch).
Monochrome/white versions look best — the page renders them at ~9% opacity
tinted by a soft glow in the league's accent color.
